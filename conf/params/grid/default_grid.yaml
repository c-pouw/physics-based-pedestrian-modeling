defaults:
  - bins: default_bins_with_r_from_list
name: dxy${params.grid.spatial_cell_size}_r${params.grid.r.list}_ntheta${params.grid.theta.segments}
spatial_cell_size: 0.2
x:
  step: ${params.grid.spatial_cell_size}
y:
  step: ${params.grid.spatial_cell_size}
r:
  min: 0
  max: 1.8
  step: 0.6
  list: [0,0.4,1.6,2.8]
theta:
  min_multiple_pi: -0.25
  min: ${parse_pi:${params.grid.theta.min_multiple_pi}}
  segments: 4
